{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema=backend/prisma/schema.prisma && node backend/dist/prisma/seed.js && node backend/dist/src/index.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
