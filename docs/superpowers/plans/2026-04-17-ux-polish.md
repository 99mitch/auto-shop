# UX Polish — FloraMini Frontend Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rendre l'interface FloraMini plus travaillée — flèches retour in-app, bottom sheet "Ajouté au panier", composants shadcn-style, cards redesignées.

**Architecture:** On installe `lucide-react`, `clsx`, `tailwind-merge` pour avoir les fondations shadcn sans CLI. On crée des composants UI légers (`Button`, `Badge`, `AddedToCartSheet`, `PageHeader`). Le Layout est mis à jour pour n'afficher son header que sur la page catalogue ; les sous-pages rendent leur propre `PageHeader` avec flèche retour. La bottom sheet remplace la navigation automatique vers `/cart` après ajout d'un produit.

**Tech Stack:** React 18, Vite, Tailwind CSS 3, lucide-react, clsx, tailwind-merge, react-router-dom v6, zustand, @twa-dev/sdk

---

## File Map

| Action | Path | Responsibility |
|--------|------|----------------|
| Create | `src/lib/utils.ts` | `cn()` helper (clsx + tailwind-merge) |
| Create | `src/components/ui/button.tsx` | Composant Button réutilisable |
| Create | `src/components/ui/badge.tsx` | Composant Badge réutilisable |
| Create | `src/components/PageHeader.tsx` | Header sticky avec flèche retour + titre |
| Create | `src/components/AddedToCartSheet.tsx` | Bottom sheet "Ajouté au panier" |
| Modify | `src/components/Layout.tsx` | Header uniquement sur `/` (catalogue) |
| Modify | `src/components/CartIcon.tsx` | Redesign avec lucide-react |
| Modify | `src/components/ProductCard.tsx` | Style bordure, bouton `+` rapide |
| Modify | `src/pages/ProductDetail.tsx` | PageHeader + AddedToCartSheet |
| Modify | `src/pages/Cart.tsx` | PageHeader |
| Modify | `src/pages/Checkout.tsx` | PageHeader |
| Modify | `src/pages/Orders.tsx` | PageHeader |
| Modify | `src/pages/OrderDetail.tsx` | PageHeader |
| Modify | `src/pages/Catalogue.tsx` | Barre de recherche polie |

---

## Task 1 : Installer les dépendances et créer `cn()`

**Files:**
- Modify: `frontend/package.json`
- Create: `frontend/src/lib/utils.ts`

- [ ] **Step 1 : Installer les packages**

```bash
cd /home/mitch/auto-shop/frontend
npm install lucide-react clsx tailwind-merge
```

Expected: packages ajoutés dans `node_modules/`, `package.json` mis à jour.

- [ ] **Step 2 : Créer `src/lib/utils.ts`**

```ts
import { clsx, type ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

- [ ] **Step 3 : Commit**

```bash
cd /home/mitch/auto-shop/frontend
git add package.json package-lock.json src/lib/utils.ts
git commit -m "feat: install lucide-react, clsx, tailwind-merge + cn() util"
```

---

## Task 2 : Composants UI — Button et Badge

**Files:**
- Create: `frontend/src/components/ui/button.tsx`
- Create: `frontend/src/components/ui/badge.tsx`

- [ ] **Step 1 : Créer `src/components/ui/button.tsx`**

```tsx
import { cn } from '../../lib/utils'
import type { ButtonHTMLAttributes } from 'react'

interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'default' | 'outline' | 'ghost'
  size?: 'sm' | 'md' | 'icon'
}

export function Button({ variant = 'default', size = 'md', className, ...props }: ButtonProps) {
  return (
    <button
      className={cn(
        'inline-flex items-center justify-center font-semibold transition-colors active:scale-95 disabled:opacity-50 disabled:pointer-events-none',
        {
          'bg-[--tg-theme-button-color,#0f172a] text-[--tg-theme-button-text-color,#fff] rounded-xl': variant === 'default',
          'border border-[#e2e8f0] bg-white text-[#475569] rounded-xl hover:bg-[#f8fafc]': variant === 'outline',
          'bg-transparent text-[#475569] rounded-xl hover:bg-[#f8fafc]': variant === 'ghost',
        },
        {
          'h-9 px-4 text-sm': size === 'md',
          'h-7 px-3 text-xs': size === 'sm',
          'h-9 w-9 p-0': size === 'icon',
        },
        className
      )}
      {...props}
    />
  )
}
```

- [ ] **Step 2 : Créer `src/components/ui/badge.tsx`**

```tsx
import { cn } from '../../lib/utils'
import type { HTMLAttributes } from 'react'

interface BadgeProps extends HTMLAttributes<'span'> {
  variant?: 'default' | 'success' | 'warning' | 'danger' | 'secondary'
}

export function Badge({ variant = 'default', className, ...props }: BadgeProps) {
  return (
    <span
      className={cn(
        'inline-flex items-center rounded-md px-2 py-0.5 text-xs font-semibold',
        {
          'bg-[#f1f5f9] text-[#475569]': variant === 'default',
          'bg-[#f0fdf4] text-[#15803d]': variant === 'success',
          'bg-[#fff7ed] text-[#c2410c]': variant === 'warning',
          'bg-[#fef2f2] text-[#b91c1c]': variant === 'danger',
          'bg-[#f1f5f9] text-[#64748b]': variant === 'secondary',
        },
        className
      )}
      {...props}
    />
  )
}
```

- [ ] **Step 3 : Commit**

```bash
cd /home/mitch/auto-shop/frontend
git add src/components/ui/
git commit -m "feat: add Button and Badge UI components"
```

---

## Task 3 : Composant `PageHeader` (flèche retour in-app)

**Files:**
- Create: `frontend/src/components/PageHeader.tsx`

Ce composant rend un header sticky avec une flèche retour et un titre. Il continue d'utiliser `useTelegramBackButton` pour le bouton natif Telegram **en plus** de la flèche visuelle.

- [ ] **Step 1 : Créer `src/components/PageHeader.tsx`**

```tsx
import { useCallback } from 'react'
import { useNavigate } from 'react-router-dom'
import { ArrowLeft } from 'lucide-react'
import { useTelegramBackButton } from '../hooks/useTelegramBackButton'
import { cn } from '../lib/utils'

interface PageHeaderProps {
  title: string
  onBack?: () => void
  right?: React.ReactNode
  className?: string
}

export default function PageHeader({ title, onBack, right, className }: PageHeaderProps) {
  const navigate = useNavigate()
  const handleBack = useCallback(() => {
    if (onBack) onBack()
    else navigate(-1)
  }, [onBack, navigate])

  useTelegramBackButton(handleBack)

  return (
    <div
      className={cn(
        'sticky top-0 z-40 flex items-center gap-3 px-3 py-3 border-b',
        className
      )}
      style={{
        backgroundColor: 'var(--tg-theme-bg-color, #fff)',
        borderColor: 'var(--tg-theme-hint-color, #e2e8f0)',
      }}
    >
      <button
        onClick={handleBack}
        className="flex h-8 w-8 shrink-0 items-center justify-center rounded-lg border transition-colors active:scale-95"
        style={{
          borderColor: 'var(--tg-theme-hint-color, #e2e8f0)',
          backgroundColor: 'var(--tg-theme-bg-color, #fff)',
          color: 'var(--tg-theme-text-color, #0f172a)',
        }}
        aria-label="Retour"
      >
        <ArrowLeft size={16} strokeWidth={2.5} />
      </button>
      <h1
        className="flex-1 text-sm font-semibold truncate"
        style={{ color: 'var(--tg-theme-text-color, #0f172a)' }}
      >
        {title}
      </h1>
      {right && <div className="shrink-0">{right}</div>}
    </div>
  )
}
```

- [ ] **Step 2 : Commit**

```bash
cd /home/mitch/auto-shop/frontend
git add src/components/PageHeader.tsx
git commit -m "feat: add PageHeader component with in-app back arrow"
```

---

## Task 4 : Composant `AddedToCartSheet` (bottom sheet)

**Files:**
- Create: `frontend/src/components/AddedToCartSheet.tsx`

Bottom sheet animée (slide-up) avec overlay, montre le produit ajouté, 2 CTA. Pas de Radix — pure CSS animation + `useEffect` pour auto-close.

- [ ] **Step 1 : Créer `src/components/AddedToCartSheet.tsx`**

```tsx
import { useEffect } from 'react'
import { useNavigate } from 'react-router-dom'
import { CheckCircle2, ArrowLeft, ShoppingCart } from 'lucide-react'
import { Button } from './ui/button'

interface AddedToCartSheetProps {
  productName: string
  productPrice: number
  onClose: () => void
}

export default function AddedToCartSheet({ productName, productPrice, onClose }: AddedToCartSheetProps) {
  const navigate = useNavigate()

  useEffect(() => {
    const timer = setTimeout(onClose, 5000)
    return () => clearTimeout(timer)
  }, [onClose])

  return (
    <>
      {/* Overlay */}
      <div
        className="fixed inset-0 z-40 bg-black/30 animate-fade-in"
        onClick={onClose}
      />
      {/* Sheet */}
      <div
        className="fixed bottom-0 left-0 right-0 z-50 rounded-t-2xl p-5 animate-slide-up"
        style={{
          backgroundColor: 'var(--tg-theme-bg-color, #fff)',
          boxShadow: '0 -8px 30px rgba(0,0,0,0.12)',
        }}
      >
        {/* Handle */}
        <div className="mx-auto mb-4 h-1 w-10 rounded-full bg-[#e2e8f0]" />

        {/* Product info */}
        <div className="flex items-center gap-3 mb-5">
          <div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-[#f0fdf4]">
            <CheckCircle2 size={20} className="text-[#22c55e]" />
          </div>
          <div>
            <p className="text-sm font-bold" style={{ color: 'var(--tg-theme-text-color, #0f172a)' }}>
              Ajouté au panier !
            </p>
            <p className="text-xs" style={{ color: 'var(--tg-theme-hint-color, #64748b)' }}>
              {productName} · €{productPrice.toFixed(2)}
            </p>
          </div>
        </div>

        {/* CTAs */}
        <div className="flex gap-3">
          <Button variant="outline" className="flex-1 gap-2" onClick={onClose}>
            <ArrowLeft size={14} />
            Continuer
          </Button>
          <Button
            className="flex-1 gap-2"
            onClick={() => navigate('/cart')}
          >
            <ShoppingCart size={14} />
            Voir le panier
          </Button>
        </div>
      </div>
    </>
  )
}
```

- [ ] **Step 2 : Ajouter les animations Tailwind dans `tailwind.config.js`**

Lire le fichier de config Tailwind existant :

```bash
cat /home/mitch/auto-shop/frontend/tailwind.config.js 2>/dev/null || cat /home/mitch/auto-shop/frontend/tailwind.config.ts 2>/dev/null
```

Puis ajouter dans `extend`:

```js
// Dans tailwind.config.js — extend.keyframes
keyframes: {
  'fade-in': {
    '0%': { opacity: '0' },
    '100%': { opacity: '1' },
  },
  'slide-up': {
    '0%': { transform: 'translateY(100%)' },
    '100%': { transform: 'translateY(0)' },
  },
},
// Dans extend.animation
animation: {
  'fade-in': 'fade-in 0.2s ease-out',
  'slide-up': 'slide-up 0.3s cubic-bezier(0.32, 0.72, 0, 1)',
},
```

- [ ] **Step 3 : Commit**

```bash
cd /home/mitch/auto-shop/frontend
git add src/components/AddedToCartSheet.tsx tailwind.config.*
git commit -m "feat: add AddedToCartSheet bottom sheet component"
```

---

## Task 5 : Mettre à jour `Layout.tsx`

**Files:**
- Modify: `frontend/src/components/Layout.tsx`
- Modify: `frontend/src/components/CartIcon.tsx`

Le header du Layout ne s'affiche que sur `/` (la catalogue). Les sous-pages rendent leur propre `PageHeader`. Le `CartIcon` est redesigné.

- [ ] **Step 1 : Redesigner `CartIcon.tsx`**

Remplacer le contenu complet par :

```tsx
import { useNavigate, useLocation } from 'react-router-dom'
import { ShoppingBag } from 'lucide-react'
import { useCartStore } from '../stores/cart'

export default function CartIcon() {
  const navigate = useNavigate()
  const location = useLocation()
  const totalItems = useCartStore((s) => s.totalItems())

  if (location.pathname === '/cart') return null

  return (
    <button
      onClick={() => navigate('/cart')}
      className="relative flex h-9 w-9 items-center justify-center rounded-xl transition-colors active:scale-95"
      style={{
        backgroundColor: 'var(--tg-theme-secondary-bg-color, #f1f5f9)',
        color: 'var(--tg-theme-text-color, #0f172a)',
      }}
      aria-label="Panier"
    >
      <ShoppingBag size={20} />
      {totalItems > 0 && (
        <span className="absolute -top-1 -right-1 flex h-4 w-4 items-center justify-center rounded-full bg-[#ef4444] text-[10px] font-bold text-white leading-none">
          {totalItems > 9 ? '9+' : totalItems}
        </span>
      )}
    </button>
  )
}
```

- [ ] **Step 2 : Mettre à jour `Layout.tsx`**

Remplacer le contenu complet par :

```tsx
import { Outlet, useLocation } from 'react-router-dom'
import { useTheme } from '../hooks/useTheme'
import CartIcon from './CartIcon'

export default function Layout() {
  useTheme()
  const { pathname } = useLocation()
  const isAdmin = pathname.startsWith('/admin')
  const isCatalogue = pathname === '/'

  return (
    <div
      className="min-h-screen"
      style={{
        backgroundColor: 'var(--tg-theme-bg-color, #ffffff)',
        color: 'var(--tg-theme-text-color, #000000)',
      }}
    >
      {isCatalogue && (
        <header
          className="sticky top-0 z-40 flex items-center justify-between px-4 py-3 border-b"
          style={{
            borderColor: 'var(--tg-theme-hint-color, #e2e8f0)',
            backgroundColor: 'var(--tg-theme-bg-color, #fff)',
          }}
        >
          <div className="flex items-center gap-2">
            <div className="h-2 w-2 rounded-full bg-[#22c55e]" />
            <h1
              className="text-base font-bold tracking-tight"
              style={{ color: 'var(--tg-theme-text-color, #0f172a)' }}
            >
              FloraMini
            </h1>
          </div>
          <CartIcon />
        </header>
      )}
      {isAdmin && (
        <header
          className="sticky top-0 z-40 flex items-center justify-between px-4 py-3 border-b"
          style={{
            borderColor: 'var(--tg-theme-hint-color, #e2e8f0)',
            backgroundColor: 'var(--tg-theme-bg-color, #fff)',
          }}
        >
          <h1 className="text-base font-bold" style={{ color: 'var(--tg-theme-text-color, #0f172a)' }}>
            Admin
          </h1>
        </header>
      )}
      <main className="pb-24">
        <Outlet />
      </main>
    </div>
  )
}
```

- [ ] **Step 3 : Commit**

```bash
cd /home/mitch/auto-shop/frontend
git add src/components/Layout.tsx src/components/CartIcon.tsx
git commit -m "feat: update Layout header (catalogue only) + redesign CartIcon"
```

---

## Task 6 : Redesigner `ProductCard.tsx`

**Files:**
- Modify: `frontend/src/components/ProductCard.tsx`

Card avec bordure fine, badge de stock, bouton `+` visible, sans fond plein.

- [ ] **Step 1 : Remplacer `ProductCard.tsx`**

```tsx
import { Plus } from 'lucide-react'
import type { Product } from 'floramini-types'
import { Badge } from './ui/badge'

interface Props {
  product: Product
  onClick: () => void
  onQuickAdd?: () => void
}

export default function ProductCard({ product, onClick, onQuickAdd }: Props) {
  return (
    <div
      className="flex flex-col rounded-xl overflow-hidden border transition-shadow hover:shadow-md active:scale-[0.98]"
      style={{
        borderColor: 'var(--tg-theme-hint-color, #e2e8f0)',
        backgroundColor: 'var(--tg-theme-bg-color, #fff)',
      }}
    >
      <button onClick={onClick} className="flex-1 text-left">
        <div className="relative">
          <img
            src={product.imageUrl}
            alt={product.name}
            className="w-full h-36 object-cover"
            loading="lazy"
          />
          {product.stock === 0 && (
            <div className="absolute inset-0 bg-black/40 flex items-center justify-center">
              <Badge variant="danger">Rupture</Badge>
            </div>
          )}
          {product.stock > 0 && product.stock <= 3 && (
            <div className="absolute top-2 left-2">
              <Badge variant="warning">Plus que {product.stock}</Badge>
            </div>
          )}
        </div>
        <div className="p-3 pb-2">
          <p
            className="text-xs font-semibold leading-tight mb-0.5 line-clamp-2"
            style={{ color: 'var(--tg-theme-text-color, #0f172a)' }}
          >
            {product.name}
          </p>
          {product.category && (
            <p className="text-[10px] mb-1" style={{ color: 'var(--tg-theme-hint-color, #64748b)' }}>
              {product.category.name}
            </p>
          )}
        </div>
      </button>
      <div className="flex items-center justify-between px-3 pb-3">
        <span
          className="text-sm font-bold"
          style={{ color: 'var(--tg-theme-text-color, #0f172a)' }}
        >
          €{product.price.toFixed(2)}
        </span>
        {product.stock > 0 && onQuickAdd && (
          <button
            onClick={(e) => { e.stopPropagation(); onQuickAdd() }}
            className="flex h-7 w-7 items-center justify-center rounded-lg transition-colors active:scale-95"
            style={{
              backgroundColor: 'var(--tg-theme-button-color, #0f172a)',
              color: 'var(--tg-theme-button-text-color, #fff)',
            }}
            aria-label={`Ajouter ${product.name} au panier`}
          >
            <Plus size={14} strokeWidth={2.5} />
          </button>
        )}
      </div>
    </div>
  )
}
```

- [ ] **Step 2 : Commit**

```bash
cd /home/mitch/auto-shop/frontend
git add src/components/ProductCard.tsx
git commit -m "feat: redesign ProductCard with border style and quick-add button"
```

---

## Task 7 : Mettre à jour `Catalogue.tsx`

**Files:**
- Modify: `frontend/src/pages/Catalogue.tsx`

Passer `onQuickAdd` aux ProductCard, polir la barre de recherche avec `lucide-react`.

- [ ] **Step 1 : Remplacer `Catalogue.tsx`**

```tsx
import { useState, useCallback } from 'react'
import { useNavigate } from 'react-router-dom'
import { useQuery, useQueryClient } from '@tanstack/react-query'
import { Search, X } from 'lucide-react'
import { api } from '../lib/api'
import type { Product, Category } from 'floramini-types'
import { useCartStore } from '../stores/cart'
import LoadingSkeleton from '../components/LoadingSkeleton'
import ProductCard from '../components/ProductCard'
import AddedToCartSheet from '../components/AddedToCartSheet'
import WebApp from '@twa-dev/sdk'

const ALL_SLUG = 'all'

export default function Catalogue() {
  const navigate = useNavigate()
  const addItem = useCartStore((s) => s.addItem)
  const [activeCategory, setActiveCategory] = useState(ALL_SLUG)
  const [search, setSearch] = useState('')
  const [addedProduct, setAddedProduct] = useState<Product | null>(null)

  const { data: categories = [] } = useQuery<Category[]>({
    queryKey: ['categories'],
    queryFn: () => api.get('/api/categories').then((r) => r.data),
    staleTime: 5 * 60 * 1000,
  })

  const { data: products = [], isLoading } = useQuery<Product[]>({
    queryKey: ['products', activeCategory, search],
    queryFn: () => {
      const params = new URLSearchParams()
      if (activeCategory !== ALL_SLUG) params.set('category', activeCategory)
      if (search) params.set('search', search)
      return api.get(`/api/products?${params}`).then((r) => r.data)
    },
    staleTime: 5 * 60 * 1000,
  })

  const handleQuickAdd = useCallback((product: Product) => {
    addItem({
      productId: product.id,
      productName: product.name,
      productImageUrl: product.imageUrl,
      unitPrice: product.price,
      options: {},
    })
    WebApp.HapticFeedback?.notificationOccurred('success')
    setAddedProduct(product)
  }, [addItem])

  return (
    <div className="px-4 py-4">
      {/* Search */}
      <div
        className="flex items-center gap-2 rounded-xl px-3 py-2.5 mb-4"
        style={{ backgroundColor: 'var(--tg-theme-secondary-bg-color, #f3f4f6)' }}
      >
        <Search size={14} style={{ color: 'var(--tg-theme-hint-color, #9ca3af)' }} />
        <input
          type="text"
          placeholder="Rechercher une fleur..."
          value={search}
          onChange={(e) => setSearch(e.target.value)}
          className="flex-1 bg-transparent text-sm outline-none"
          style={{ color: 'var(--tg-theme-text-color, #0f172a)' }}
        />
        {search && (
          <button onClick={() => setSearch('')} aria-label="Effacer">
            <X size={14} style={{ color: 'var(--tg-theme-hint-color, #9ca3af)' }} />
          </button>
        )}
      </div>

      {/* Category tabs */}
      <div className="flex gap-2 overflow-x-auto pb-2 mb-4 scrollbar-hide">
        {[{ slug: ALL_SLUG, name: 'Tous' }, ...categories].map((cat) => (
          <button
            key={cat.slug}
            onClick={() => setActiveCategory(cat.slug)}
            className="shrink-0 rounded-lg px-3 py-1.5 text-xs font-semibold transition-colors"
            style={
              activeCategory === cat.slug
                ? {
                    backgroundColor: 'var(--tg-theme-button-color, #0f172a)',
                    color: 'var(--tg-theme-button-text-color, #fff)',
                  }
                : {
                    border: '1px solid var(--tg-theme-hint-color, #e2e8f0)',
                    color: 'var(--tg-theme-hint-color, #64748b)',
                  }
            }
          >
            {cat.name}
          </button>
        ))}
      </div>

      {/* Product grid */}
      {isLoading ? (
        <div className="grid grid-cols-2 gap-3">
          {Array.from({ length: 6 }).map((_, i) => (
            <LoadingSkeleton key={i} className="h-52 rounded-xl" />
          ))}
        </div>
      ) : products.length === 0 ? (
        <div className="flex flex-col items-center justify-center py-16 text-center">
          <span className="text-5xl mb-3">🌿</span>
          <p className="text-sm" style={{ color: 'var(--tg-theme-hint-color, #6b7280)' }}>
            Aucun produit trouvé
          </p>
        </div>
      ) : (
        <div className="grid grid-cols-2 gap-3">
          {products.map((product) => (
            <ProductCard
              key={product.id}
              product={product}
              onClick={() => navigate(`/product/${product.id}`)}
              onQuickAdd={() => handleQuickAdd(product)}
            />
          ))}
        </div>
      )}

      {/* Bottom sheet */}
      {addedProduct && (
        <AddedToCartSheet
          productName={addedProduct.name}
          productPrice={addedProduct.price}
          onClose={() => setAddedProduct(null)}
        />
      )}
    </div>
  )
}
```

- [ ] **Step 2 : Commit**

```bash
cd /home/mitch/auto-shop/frontend
git add src/pages/Catalogue.tsx
git commit -m "feat: update Catalogue with polished search and quick-add"
```

---

## Task 8 : Mettre à jour `ProductDetail.tsx`

**Files:**
- Modify: `frontend/src/pages/ProductDetail.tsx`

Supprimer `useTelegramBackButton` (délégué à `PageHeader`), remplacer la navigation auto vers `/cart` par l'affichage de la `AddedToCartSheet`.

- [ ] **Step 1 : Remplacer `ProductDetail.tsx`**

```tsx
import { useState, useCallback } from 'react'
import { useParams } from 'react-router-dom'
import { useQuery } from '@tanstack/react-query'
import WebApp from '@twa-dev/sdk'
import { api } from '../lib/api'
import type { Product } from 'floramini-types'
import { useCartStore } from '../stores/cart'
import { useTelegramMainButton } from '../hooks/useTelegramMainButton'
import { Badge } from '../components/ui/badge'
import PageHeader from '../components/PageHeader'
import AddedToCartSheet from '../components/AddedToCartSheet'
import LoadingSkeleton from '../components/LoadingSkeleton'
import CartIcon from '../components/CartIcon'

export default function ProductDetail() {
  const { id } = useParams<{ id: string }>()
  const addItem = useCartStore((s) => s.addItem)
  const [activeImage, setActiveImage] = useState(0)
  const [showSheet, setShowSheet] = useState(false)

  const { data: product, isLoading } = useQuery<Product>({
    queryKey: ['product', id],
    queryFn: () => api.get(`/api/products/${id}`).then((r) => r.data),
    staleTime: 5 * 60 * 1000,
  })

  const handleAddToCart = useCallback(() => {
    if (!product) return
    addItem({
      productId: product.id,
      productName: product.name,
      productImageUrl: product.imageUrl,
      unitPrice: product.price,
      options: {},
    })
    WebApp.HapticFeedback?.notificationOccurred('success')
    setShowSheet(true)
  }, [product, addItem])

  useTelegramMainButton(
    product ? `Ajouter au panier — €${product.price.toFixed(2)}` : 'Chargement...',
    handleAddToCart,
    !!product && product.stock > 0 && !showSheet
  )

  if (isLoading) {
    return (
      <>
        <PageHeader title="Produit" />
        <div className="p-4">
          <LoadingSkeleton className="h-64 w-full mb-4" />
          <LoadingSkeleton className="h-6 w-3/4 mb-2" />
          <LoadingSkeleton className="h-4 w-full mb-1" />
          <LoadingSkeleton className="h-4 w-2/3" />
        </div>
      </>
    )
  }

  if (!product) {
    return (
      <>
        <PageHeader title="Produit introuvable" />
        <div className="flex flex-col items-center justify-center py-16">
          <span className="text-5xl mb-3">🌿</span>
          <p className="text-sm" style={{ color: 'var(--tg-theme-hint-color, #6b7280)' }}>
            Produit introuvable
          </p>
        </div>
      </>
    )
  }

  const allImages = [product.imageUrl, ...product.images]

  return (
    <>
      <PageHeader title={product.name} right={<CartIcon />} />

      {/* Image gallery */}
      <div className="relative bg-[#f8fafc]">
        <img
          src={allImages[activeImage]}
          alt={product.name}
          className="w-full h-72 object-cover"
        />
        {allImages.length > 1 && (
          <div className="absolute bottom-3 left-0 right-0 flex justify-center gap-2">
            {allImages.map((_, i) => (
              <button
                key={i}
                onClick={() => setActiveImage(i)}
                className={`w-2 h-2 rounded-full transition-all ${
                  i === activeImage ? 'bg-white scale-125' : 'bg-white/60'
                }`}
              />
            ))}
          </div>
        )}
      </div>

      <div className="p-4">
        <div className="flex items-start justify-between mb-2">
          <h1
            className="text-xl font-bold flex-1 mr-2 tracking-tight"
            style={{ color: 'var(--tg-theme-text-color, #0f172a)' }}
          >
            {product.name}
          </h1>
          <span
            className="text-2xl font-bold shrink-0 tracking-tight"
            style={{ color: 'var(--tg-theme-text-color, #0f172a)' }}
          >
            €{product.price.toFixed(2)}
          </span>
        </div>

        {product.category && (
          <Badge variant="default" className="mb-3">{product.category.name}</Badge>
        )}

        <p
          className="text-sm leading-relaxed mb-4"
          style={{ color: 'var(--tg-theme-hint-color, #64748b)' }}
        >
          {product.description}
        </p>

        {product.stock === 0 ? (
          <Badge variant="danger" className="w-full justify-center py-2">
            Rupture de stock — non disponible actuellement
          </Badge>
        ) : product.stock <= 5 ? (
          <Badge variant="warning" className="w-full justify-center py-2">
            ⚠️ Plus que {product.stock} en stock !
          </Badge>
        ) : (
          <Badge variant="success" className="w-full justify-center py-2">
            ✓ En stock · {product.stock} disponibles
          </Badge>
        )}
      </div>

      {showSheet && (
        <AddedToCartSheet
          productName={product.name}
          productPrice={product.price}
          onClose={() => setShowSheet(false)}
        />
      )}
    </>
  )
}
```

- [ ] **Step 2 : Commit**

```bash
cd /home/mitch/auto-shop/frontend
git add src/pages/ProductDetail.tsx
git commit -m "feat: add PageHeader + AddedToCartSheet to ProductDetail"
```

---

## Task 9 : Ajouter `PageHeader` aux sous-pages restantes

**Files:**
- Modify: `frontend/src/pages/Cart.tsx`
- Modify: `frontend/src/pages/Checkout.tsx`
- Modify: `frontend/src/pages/Orders.tsx`
- Modify: `frontend/src/pages/OrderDetail.tsx`

Supprimer `useTelegramBackButton` de chaque page (délégué à `PageHeader`), ajouter `<PageHeader>` en haut.

- [ ] **Step 1 : Mettre à jour `Cart.tsx`**

Retirer l'import `useTelegramBackButton`. Remplacer le `<div className="p-4">` racine par :

```tsx
// Ajouter en haut du fichier :
import PageHeader from '../components/PageHeader'
import CartIcon from '../components/CartIcon'

// Supprimer :
// import { useTelegramBackButton } from '../hooks/useTelegramBackButton'
// useTelegramBackButton(useCallback(() => navigate(-1), [navigate]))

// Dans le JSX du cas items.length === 0, ajouter PageHeader avant le div :
return (
  <>
    <PageHeader title="Mon panier" />
    <div className="flex flex-col items-center ...">
      ...
    </div>
  </>
)

// Dans le JSX principal, wrapper avec Fragment :
return (
  <>
    <PageHeader title="Mon panier" />
    <div className="p-4">
      ...
    </div>
  </>
)
```

- [ ] **Step 2 : Mettre à jour `Checkout.tsx`**

```tsx
// Ajouter l'import :
import PageHeader from '../components/PageHeader'

// Supprimer :
// import { useTelegramBackButton } from '../hooks/useTelegramBackButton'
// useTelegramBackButton(useCallback(() => navigate(-1), [navigate]))

// Supprimer aussi l'import useCallback si plus utilisé ailleurs dans ce fichier

// Wrapper le return :
return (
  <>
    <PageHeader title="Livraison" />
    <div className="p-4 space-y-4">
      {/* Supprimer le <h2>Livraison</h2> qui fait doublon */}
      ...
    </div>
  </>
)
```

- [ ] **Step 3 : Mettre à jour `Orders.tsx`**

```tsx
// Ajouter l'import :
import PageHeader from '../components/PageHeader'
import { useNavigate } from 'react-router-dom'

// Supprimer :
// import { useTelegramBackButton } from '../hooks/useTelegramBackButton'
// useTelegramBackButton(...)

// Cas isLoading :
return (
  <>
    <PageHeader title="Mes commandes" onBack={() => navigate('/')} />
    <div className="p-4 space-y-3">...</div>
  </>
)

// Cas orders vides :
return (
  <>
    <PageHeader title="Mes commandes" onBack={() => navigate('/')} />
    <div className="flex flex-col items-center ...">...</div>
  </>
)

// Cas normal — supprimer le <h2>Mes commandes</h2> :
return (
  <>
    <PageHeader title="Mes commandes" onBack={() => navigate('/')} />
    <div className="p-4">
      <div className="space-y-3">...</div>
    </div>
  </>
)
```

- [ ] **Step 4 : Mettre à jour `OrderDetail.tsx`**

```tsx
// Ajouter l'import :
import PageHeader from '../components/PageHeader'

// Supprimer :
// import { useTelegramBackButton } from '../hooks/useTelegramBackButton'
// useTelegramBackButton(useCallback(() => navigate('/orders'), [navigate]))

// Cas isLoading :
return (
  <>
    <PageHeader title="Commande" onBack={() => navigate('/orders')} />
    <div className="p-4 space-y-3">...</div>
  </>
)

// Cas normal — le header + StatusBadge existant devient :
return (
  <>
    <PageHeader
      title={`Commande #${order.id}`}
      onBack={() => navigate('/orders')}
      right={<StatusBadge status={order.status} />}
    />
    <div className="p-4">
      {/* Supprimer le bloc flex items-center justify-between mb-4 qui faisait le même rôle */}
      ...
    </div>
  </>
)
```

- [ ] **Step 5 : Commit**

```bash
cd /home/mitch/auto-shop/frontend
git add src/pages/Cart.tsx src/pages/Checkout.tsx src/pages/Orders.tsx src/pages/OrderDetail.tsx
git commit -m "feat: add PageHeader with back arrow to Cart, Checkout, Orders, OrderDetail"
```

---

## Task 10 : Vérification finale

- [ ] **Step 1 : Lancer le dev server**

```bash
cd /home/mitch/auto-shop/frontend
npm run dev
```

- [ ] **Step 2 : Vérifier que TypeScript compile**

```bash
cd /home/mitch/auto-shop/frontend
npx tsc --noEmit
```

Expected: 0 erreurs.

- [ ] **Step 3 : Corriger les éventuelles erreurs TS**

Si `HTMLAttributes<'span'>` dans `badge.tsx` provoque une erreur, remplacer par `HTMLAttributes<HTMLSpanElement>`.

- [ ] **Step 4 : Vérifier visuellement les écrans**

Ouvrir http://localhost:5173 et tester :
1. Catalogue — header FloraMini + search + categories + cards avec bouton `+`
2. Cliquer `+` sur une card → bottom sheet s'affiche, disparaît après 5s
3. Cliquer sur une card → ProductDetail avec flèche retour dans header
4. Cliquer "Ajouter au panier" → bottom sheet avec "Continuer" et "Voir le panier"
5. Panier → header "Mon panier" avec flèche retour
6. Checkout → header "Livraison" avec flèche retour
7. Commandes → header "Mes commandes" avec flèche retour

- [ ] **Step 5 : Commit final si corrections**

```bash
cd /home/mitch/auto-shop/frontend
git add -p
git commit -m "fix: typescript and visual corrections after UX polish"
```
