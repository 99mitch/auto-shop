{
  "name": "floramini",
  "private": true,
  "workspaces": ["packages/*", "frontend", "backend", "bot"],
  "scripts": {
    "dev": "concurrently -n frontend,backend,bot \"npm run dev -w frontend\" \"npm run dev -w backend\" \"npm run polling -w bot\"",
    "seed": "npm run seed -w backend",
    "test": "npm run test -w backend",
    "tunnel": "~/.local/bin/cloudflared tunnel --url http://localhost:5173"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
